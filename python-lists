#lets make some lists about our pizzas
toppings = ['pepperoni', 'pineapple', 'cheese', 'sausage', 'olives', 'anchovies', 'mushrooms']
print(toppings)

prices = [2, 6, 1, 3, 2, 7, 2]
print(prices)

#lets provide a welcome message about our pizzas!
num_pizzas = len(toppings)
print(num_pizzas)
print("We sell "+ str(num_pizzas)+ " different kinds of pizza!")

#lets combine our pizza lists into a single list:
pizzas = list(zip(prices, toppings))
print(pizzas)

#lets sort that list 
sorted_pizzas = sorted(pizzas)
print(sorted_pizzas)

#this is the cheapest pizza
cheapest_pizza = sorted_pizzas[0]
print(cheapest_pizza)

#this is our most expensive pizza
priciest_pizza = sorted_pizzas[-1]
print(priciest_pizza)

#lets show our three cheapest pizzas:
three_cheapest = sorted_pizzas[0:3]
print(three_cheapest)

#lets see how many two dollar pizzas we have:
num_two_dollar_slices = prices.count(2)
print(num_two_dollar_slices)











