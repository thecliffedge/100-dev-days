


def ground_ship(weight):
  if weight <= 2:
    cost = (1.50 * weight) + 20
  elif weight >2 and weight <= 6:
    cost = (3.00 * weight) + 20
  elif weight >6 and weight <=10:
    cost = (4.00 * weight) + 20
  else:
    cost = (4.75 * weight) + 20
  
  return cost

#print(ground_ship(8.4))

premium_ground = 125

def drone_ship(weight):
  if weight <= 2:
    cost = (4.50 * weight)
  elif weight >2 and weight <= 6:
    cost = (9.00 * weight)
  elif weight >6 and weight <=10:
    cost = (12.00 * weight)
  else:
    cost = (14.25 * weight)
  
  return cost

#print(drone_ship(1.5))


def print_cheapest_shipping_cost(weight):
  
  ground = shipping_cost_ground(weight)
  drone = shipping_cost_drone(weight)
  premium = shipping_cost_premium
  
  if ground < premium and ground < drone:
    method = "standard ground"
    cost = 
  
  
  print(
    "The cheapest option available is %.2f with $s shipping."
    % (cost, method)
  )
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  ship_by_drone = "drone shipping"
  ship_by_ground = "ground shipping"
  ship_premium = "premium shipping"
  
  if drone_ship(weight) < ground_ship(weight) and drone_ship(weight) < premium_ground:
    best_price = drone_ship(weight)
    print("You should ship using "+ ship_by_drone + ", it will cost "+ str(best_price)
    
  elif ground_ship(weight) < drone_ship(weight) and ground_ship(weight) < premium_ground:
    best_price = ground_ship(weight)
    print("You should ship using "+ ship_by_ground + ", it will cost "+ str(best_price)
    
  else:
      best_price = ship_premium
      print("You should ship using "+ premium_ship + ", it will cost "+ str(best_price)
  
  return cheapest_shipping_cost(4.8)
  
  
